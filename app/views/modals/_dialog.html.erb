<dialog id="turbo-confirm" class="rounded">
  <div class="flex p-4 w-fit h-fit">
    <form method="dialog" class="flex flex-col gap-2">
      <p>are you sure?</p>
      <div>
        <button value="cancel" class="cursor-pointer bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">cancel</button>
        <button value="confirm" class="cursor-pointer bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded">confirm</button>
      </div>
    </form>
  </div>
</dialog>